import type { TransparencyScore } from "@radar-municipal/core";

/**
 * Calcula el score de transparencia para un municipio.
 * Stub: implementación real en Fase 1.
 */
export function calculateTransparencyScore(
  _municipioId: string,
  _anio: number,
  _trimestre?: number
): TransparencyScore {
  throw new Error("Not implemented: scoring engine se implementa en Fase 1");
}
